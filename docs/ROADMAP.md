# Yol haritası — kullanıcı planı (2026-09-10)

Sahip: Claude (ana sorumlu). Kaynak: kullanıcının 10 Eylül devir ve oynanış geri
bildirimi. Bu belge FINISH_PLAN ve FIRST_PERSON_PLAN'ın üstünde sıra belirler;
eski planlardaki "Vercel açılmaz", "F00'dan önce başlama" sınırları kalkmıştır.
Her adım tamamlanınca burada işaretlenir; kanıt ilgili `docs/qa/claude/*` raporunda.

Teknoloji kararı: Vite + React + Three.js/R3F, Vercel Hobby + Supabase Free
korunur. Baştan yazma yok; performans ölçümle ve hedefli düzeltmeyle alınır
(bkz. §C).

## A. Şimdi — oyun çalışsın ve oynanış "oyuna girmiş gibi" olsun

| # | İş | Durum | Not |
| --- | --- | --- | --- |
| A0 | Canlı 401: tarayıcı paketinde VITE değişkenleri yok | **yapıldı 2026-09-10** (Vercel Config değişkenleri eklendi, canlıda oda açılıyor) | Kullanıcı Vercel Production'a `VITE_SUPABASE_URL` + `VITE_SUPABASE_PUBLISHABLE_KEY` ekleyip **yeni build** alır. Kod: üretimde sessiz dev düşüşü yerine açık hata |
| A1 | 0006 rematch migration uzakta | **yapıldı 2026-09-10** | Uzak history 0001–0007 tam; 6 botla canlı oyun sonu → `play_again` → yeni gameId, `role_reveal`, revision 101→102; host olmayan ve devam eden oyun reddedildi (`scratchpad/rematch-check.mjs`) |
| A2 | Tam ekran öncelikli oyun ekranı | **yapıldı 2026-09-10** (gerçek oda kontrolü A5) | Sahne tüm ekran; sağ HTML panel kaldırılır. Alt **tuş çubuğu**: o an yapılabilen eylemler ve tuşları (1/2/3 kart, E seç, Enter gönder, Backspace vazgeç, H kimlik, V kamera, M menü). Üstte aşama/sıra **bilgi şeridi** ("Kartlar dağıtıldı", "Başkan Mert şansölye seçiyor"). Oyuncu listesi, geçmiş ve tercihler menüde (M). Telefonda alt çubuk dokunmatik düğme olur |
| A3 | CODEX-017 uygulama düzeltmeleri | **yapıldı 2026-09-10** (gerçek Pointer Lock A5) | `set_camera` niyeti app kamera durumuna; controller null → eylem yok; kilitli E = `controller.target()`; hassasiyet ayarı |
| A4 | `cards_moved` cue'su motor/projeksiyonda üretilsin | **yapıldı 2026-09-10** | Diğer oyuncuların kapalı kart bırakma animasyonu için; gizli tür/sıra taşımaz |
| A5 | Gerçek tarayıcıda Vercel kabulü | açık (yerel bot+tarayıcı tam oyun + rematch GEÇTİ 2026-09-10) | Oda aç/katıl/başlat, tam oyun, rematch, yenileme, Pointer Lock, klavye; arkadaş grubu ayrı. Kullanıcı kontrol listesi: (1) girişte "istemci: canlı" yazıyor mu, (2) lobiden oyuna geçişte sahne tam boy çiziliyor mu (ajan tarayıcısında ResizeObserver çalışmadığından doğrulanamadı), (3) telefonda alt çubuk ve sahne, (4) Oyuna odaklan → gerçek Pointer Lock ile E/sol tık |

| A6 | **Geliştirme bot modu** (kullanıcı isteği 2026-09-10) | **yapıldı 2026-09-10** (`docs/qa/claude/A6-dev-bots.md`) | Yalnız `import.meta.env.DEV` iken lobide "5 bot ekle (dev)" düğmesi. Tarayıcı içinde 5 ayrı anonim oturum açılır (kalıcı olmayan Supabase istemcileri; Supabase yoksa `dev:` kimlikleri), aynı davetle odaya katılır, hazır olur; oyun boyunca yalnız kendi sıralarında **rastgele geçerli hamle** oynar (`scratchpad/party.mjs` mantığı tarayıcıya taşınır, `apps/web/src/dev/botRunner.ts`). Kullanıcı 6. koltuktan normal akışı izler. Üretim paketine girmez (DEV ağaç sarsımı), sunucuya yeni komut eklenmez, oyun kuralı değişmez. Bot adları "Bot-…" ile ayırt edilir; sekme kapanınca botlar durur, oyun `paused` olur |

## D. Kullanıcı öncelikleri — 2026-09-11 (sırayla, adım adım)

Kullanıcı notu: "grafiklerin baya iyileştirmeye ihtiyacı var; site / oyun kur / lobi çok dandik". Bu
liste B aşamasının önüne geçer; her adım tek tek yapılır, kullanıcı görsel kabul verir, sonra sıradaki.

| # | İş | Durum | Not |
| --- | --- | --- | --- |
| D1 | **Eller: yeniden yapım** | **teslim, kullanıcı görsel kabulü bekliyor** (2026-09-11; **tur 5 perdesiz eller 2026-09-12** — parmak arası perdeler kalktı, komşu parmaklar birbirine kaynamıyor, skin sızması yok; `docs/qa/claude/D1-hands.md` “Tur 5”; **tur 6 yapışık bölge taraması 2026-09-12** — 14 kavrama × 3 açı tarandı, köşe kümeleme parça-farkındalı, parmak arası yarık (`sdf.SPLIT`), yeni `/dev/hands` inceleme sayfası; kareler `docs/qa/claude/d1/{webless-*,fused-before-after,fused-grid-after,skin-tones}.jpg`; ilk şahıs eli D3.4 ten seçimini alıyor) | Kullanıcı 2026-09-11: parmaklar "top top" görünüyor (eklem küreleri, aralarda boşluk), kart tutarken iç içe geçme var. Mevcut bölütlü/instanced el yaklaşımı (`InstancedHandModel`, `HandModel`, `rig.ts`) ile bu çözülmez; **tek parça, kemikli (skinned) el** gerekir. **Üretim Claude, dış araç yok** (Blender kurulu değil, dış model indirilmez): el yüzeyi kodda üretilir — avuç + 5 parmak × 3 falanks kapsüllerinin yumuşak birleşimi (SDF) → marching cubes (three `MarchingCubes`) → tek `BufferGeometry` (≈3–5k üçgen), her köşeye en yakın 1–2 kapsülden kemik ağırlığı → `SkinnedMesh` + `Skeleton` (bilek + 15 parmak kemiği). Geometri bir kez üretilip tüm ellerde paylaşılır (el başına ayrı iskelet); kalite kademesine göre voxel çözünürlüğü (low: daha kaba). Tutuş pozları (boş, tek kart, üç kart yelpaze, zarf, oy kartı) kemik açı seti olarak; kart düzlemi baş/işaret parmağı ucu hedeflerine göre yerleşir, parmak kıvrım sınırı ile iç içe geçme engellenir. Referans: kaliteli kart tutan oyunlar — Red Dead Redemption 2 poker, Prominence Poker, Tabletop Playground; stil paletimize uyarlanır. Entegrasyon ve animasyon (çek/seç/bırak/oy/zarf süreleri korunur) aynı işte. Kabul: koltuk kamerasında yakın plan önce/sonra kareler, 3→2→1 kart geçişleri, kart-parmak teması, eklem boşluğu yok, çizim çağrısı el başına ≤2, üretim süresi < 100 ms **Teslim:** `packages/scene/src/hands/**` (SDF 23 ilkel → kendi marching cubes → 16 kemikli `SkinnedMesh`), `handFromCard` ile el kartı taşır, yelpaze pivotu baş parmak yastığında, PublicArms pişirilmiş `restTable` pozu. El başına 2 çizim (eskiden 3), standard 4 950 üçgen / 63–78 ms üretim; koltuk FPS 60 (CPU 4×), genel masa 29 → 25. typecheck 6/6, 400 test, build geçti. Tur 2 (koordinatör): önkol/manşet inceltildi (.066 → .042/.030), dirsek aşağı-arkaya alındı, üst kol parçası kaldırıldı. Rapor ve önce/sonra kareleri: [D1-hands](qa/claude/D1-hands.md), Tur 3: manşet ucu kapatıldı, `inspect` okuma kipinde el kartı tutmuyor. Kareler `docs/qa/claude/d1/` (`before-*`, `after-*`, `after2-*`, `after3-*`). |
| D2 | **Odanın etrafı tamamlanacak** | **teslim, kullanıcı görsel kabulü bekliyor** — 2026-09-12 (Opus yan ajan; D2.1 kabuk + D2.2 mobilya + D2.3 süs tek turda). Rapor `docs/qa/claude/D2-room.md`, kareler `docs/qa/claude/d2/` (12 jpeg) | `packages/scene/src/room/` (yeni: `Room.tsx` + kabuk/mobilya/süs/doku/geometri dosyaları), eski `objects/RoomDecor.tsx` SİLİNDİ. Sekizgen oda, gece penceresi, yalnız sis. Ölçüldü: **18 çizim (eski 19 → −1)**, 12 836 üçgen, ≈11,55 MB doku, 3 nokta ışık; low 9 çizim / 2 344 üçgen / 1 ışık. 10 kişilik koltuk CPU 4×: önce 59,5 → sonra 59,9 FPS. 25 yeni test |
| D3 | **Karakterler: büyük, yuvarlak, komik; seçilebilir 8 karakter** | **D3.4 lobi karakter seçimi TESLİM (2026-09-12, Opus; D8 beklenmeden mevcut lobiye eklendi) — ⚠️ `supabase/migrations/0008_avatar.sql` KULLANICI TARAFINDAN UYGULANMALI, yoksa canlı lobide seçim hata verir**; sözleşme additive (`PlayerView.avatar`, `LobbyMember.avatar`, `set_avatar`; paket 0.2.3, `CONTRACT_VERSION` 0.2.0 sabit), sunucu `resolveAvatars` (seçmeyen koltuğa koltuk varsayılanı, çakışan varsayılanda farklı ten önerisi), lobide `AvatarPicker` (seçili karakter tek Canvas 3D + 8 kart + 3 ten, telefonda yatay kaydırma), masada `PlayerView.avatar`dan çizim, bot modu rastgele farklı karakter; typecheck 6/6 · test 788 · build geçti; rapor `docs/qa/claude/D3-characters.md`, kareler `docs/qa/claude/d3/lobby-picker-*.jpg`. D3.2–D3.3 teslim (2026-09-11). Kalan: D3.5 perf/low cilası | Kullanıcı: mevcut karakterler küçük ve beğenilmiyor; daha büyük (×1,4, oturmuş ~1,0 m), yuvarlak hatlı chibi oran, komik/samimi, gerçekçi değil; lobide seçilebilir birkaç karakter (B3 ile birleşti). Plan: `docs/design/D3-characters-plan.md` (stil, 8 karakter × 3 ten, D1 boru hattı: SDF → tek parça SkinnedMesh + yüz dokusu, bütçe ≤3 çizim/oyuncu, adımlar D3.1–D3.5). Sıra: D3.1 tasarım sayfası (Fable) → onay → kod. Rol ima eden renk/şekil yok |
| D4 | **Hitler faşisti görüyor mu — kural kontrolü** | **kontrol edildi 2026-09-11** | Kod resmî kurala uygun: 5–6 oyuncuda Hitler faşisti bilir, 7–10'da bilmez (`ROLE_SETUPS.hitlerKnowsFascists`, `knownPlayersFor`). Faşistler her zaman birbirini ve Hitler'i bilir. Rol panelinde "bilinen oyuncular" listesi gösteriliyor; canlıda 5–6 kişilik oyunda göz kontrolü kullanıcıda |
| D5 | **Oy durumu ve oy sonuçları başların üstünde + HUD** | **teslim, kullanıcı görsel kabulü bekliyor** 2026-09-11 (Opus yan ajan) — rapor `docs/qa/claude/D5-votes.md`, kareler `docs/qa/claude/d5/` | Kullanıcı 2026-09-11: "oylamalarda milletin evet/hayır verdiğini görmüyoruz; isimler ve oylar görünsün". (1) Oylama fazında her avatarın üstünde kameraya bakan rozet: oy verdi → tik, bekliyor → soluk; yalnız `hasVoted`. (2) Sonuç fazında aynı rozet EVET/HAYIR (yeşil tik / kırmızı çarpı, ballot sanatıyla aynı dil), `lastElection.votes`'tan; seçim sonucu fazı boyunca + en az 5 s. (3) HUD durum şeridi: sonuçta "Evet 4 · Hayır 3" ve isim listesi; oylamada bekleyen isimler. (4) Genel masa kamerasında da okunur. Kamu bilgisi, sızıntı yok; koltuk önündeki küçük oy kartı çevirme sürer. UYGULAMA NOTU: motorda `election_result` aşaması YOK (`resolveVote` doğrudan kanun aşamasına geçer), bu yüzden görünürlük `votes_revealed` cue'sunun geldiği andan itibaren 5 sn olarak bağlandı; rozet en az 24 px yüksek kalacak şekilde ölçekleniyor. Yeni fixture: `voting-waiting`, `election-result-close` |
| D6 | **İsim etiketleri: baş üstü etiket** | **teslim, kullanıcı görsel kabulü bekliyor** (2026-09-11; Fable tasarım → Opus kod; rapor `docs/qa/claude/D6-nameplate.md`) | Kullanıcı 2026-09-11: masadaki yan duran isim kartları "çok bozuk duruyor"; karakterin üstüne taşınsın. Karar: masa üstü plaka kalkar; her avatarın başının üstünde kameraya bakan tek etiket = isim + makam rozeti (BAŞKAN/ŞANSÖLYE/ADAY) + durum (elendi soluk, bağlantı yok ikonu) + **D5 oy rozeti aynı etikete gömülür**; hedef seçilebilirken altın çerçeve, seçiliyken dolu altın; yerel oyuncu koltuk kamerasında kendi etiketini görmez; genel masada min. piksel boyutu; tıklama/hedefleme etiket + karakter gövdesi. Koltuk numarası küçük. Önce SVG taslak (durum sayfası) → kod |
| D7 | **Genel grafik iyileştirme** | açık | Malzeme/ışık/gölge cilası, masa ve kart dokuları, post-process yok (perf). D1–D3, D6 sonrası tek tur |
| D9 | **Faz/olay duyuruları ekran ortasında** | **teslim, kullanıcı görsel kabulü bekliyor** — 2026-09-11 (Opus yan ajan), `docs/qa/claude/D9-announcements.md` | Kullanıcı 2026-09-11: "Eylül şansölye adayını seçiyor / kart seçiyor tarzı bildirimler; oylamada herkeste ekranın ortasında OYLAMA yazsın, takip artar". Her faz değişiminde ve önemli olayda (oylar açıldı, kanun kondu, yetki, infaz, kaos, veto, oyun bitti) 2,5–3 s büyük başlık + alt satır, ekranın üst-orta bölgesinde (eller/kartlar kapanmaz), sıra sendeyse altın vurgu; aynı fazı tekrarlamaz; tıklamayı engellemez; telefon ve reduced motion uyumlu; B1 bilgi metinleri bu iş ile başlar |
| D10 | **Politika tahtaları: orijinal oyuna sadık yuvalar** | **teslim, kullanıcı görsel kabulü bekliyor** (2026-09-11; Fable tasarım → Opus kod; rapor `docs/qa/claude/D10-boards.md`) | Kullanıcı 2026-09-11: "ortadaki faşist/liberal ana kartlar değişmeli; Hitler bölgesi yazmalı, mermi işareti vb. orijinaldeki gibi". Faşist tahta: yuva başına yetki ikonu (`BOARD_LAYOUTS.fascistPowers`: büyüteç inceleme, özel seçim, deste tepesine bakma, infaz), 3. yuvadan itibaren "Hitler şansölye seçilirse faşistler kazanır" şeridi, 5. yuvada "Veto açıldı", 6. yuva faşist zafer; üç oyuncu düzeni (5–6 / 7–8 / 9–10). Liberal tahta: 5. yuva liberal zafer; seçim sayacı ile kaos notu. Türkçe, stilize, palet uyumlu; tuvalde çizilir (Path2D ikonlar) |
| D11 | **Tam ekran deneyimi** | **teslim, kullanıcı doğrulaması bekliyor** — 2026-09-11 (Opus), `docs/qa/claude/D11-fullscreen.md` | Kullanıcı: kart seçip Enter ile onaylayınca tam ekrandan çıkıyor; tam ekranda sağ üst düğmeler üst üste biniyor. Teşhis: (1) "Oyuna odaklan" düğmesi fare tıklamasından sonra odağı tutuyor, Enter düğmeyi tıklayıp "Odağı bırak" çalıştırıyor; (2) sahne kamera düğmeleri (`.st-inspection-nav`), HUD `game__top-actions` ve `Özel bilgin` paneli aynı köşede sabit ofsetlerle çakışıyor. İş: odak modeli (immersive'de odak oyun kabında; HUD düğmeleri odak tutmaz), tek sağ üst araç çubuğu (kamera + odak + menü tek satır, panel altına iner), tam ekranda HUD'un hareketsizlikte gizlenmesi, `:fullscreen` düzen ve safe-area, klavye ipuçları; testler |
| D12 | **İnfaz sahnesi: silah, ateş, çöken karakter** | **tur 5 kodda (silah HD)** — önceki: teslim, kullanıcı görsel kabulü bekliyor** — 2026-09-12 (Fable tasarım `docs/design/D12-execution.md` → Opus kod; **tur 2** koordinatör geri bildirimiyle silah ele göre yeniden oranlandı (1,31 el, tambur + tetik korkuluğu görünür, kabza avuç içinde, yeni flaş); rapor `docs/qa/claude/D12-execution.md`, kareler `docs/qa/claude/d12/*-t2.jpg`; SDF silah 646 üçgen, boşta çizim artışı yok, **tur 3** (kullanıcı canlı geri bildirimi): silah hedef seçilirken de elde (`executive_action` + `execution` yetkisi → hazır poz, koreografi oradan başlar) ve HUD seçenek rakamları 1–9'a çıktı; **tur 4** (kullanıcı): silah 1,31 → **1,9 el** büyüdü, patlama animasyonu (flaş büyüyüp söner, 4 duman sprite'ı, 8 kıvılcım, geri tepme 12°, hedefte 80 ms sarsılma) ve yeni gür `shot` sesi (0,40 s, transient + düşen vuruş + kuyruk + yankı) eklendi; ses varsayılanı AÇIK; **tur 5** (kullanıcı "HD yapsaydık"): silah 686 → **2 790 üçgen** (6,5 mm voxel + 11,5 mm kümeleme + Newton yansıtma + SDF gradyan normalleri), horoz/nişangâh/pim/vida ayrıntıları, D3 kenar-yumuşatmalı renk sınırları, köşe başına metal/ahşap/pirinç PBR + kendi küçük prosedürel envMap'i (tek çizim); HD geometri `requestIdleCallback` kuyruğunda, ilk kare kaba sürüm; typecheck 6/6 · test 681 · build geçti) | Kullanıcı 2026-09-11: infazda başkanın eline silah gelsin, karşıya ateş etsin; nişan yok, hedef HUD'dan seçilir. Oyuncak tarzı SDF tabanca (≤700 üçgen, tembel üretim), `holdGun` kavrama, `player_eliminated` cue'su 2,6 s koreografi (kalk–nişan–ateş–çök–in), namlu flaşı, `shot` sesi, hedef karakter kalıcı çökük poz + `ko` ifadesi, vurulan oyuncuya vinyet + "VURULDUN", duyuru ateş anında, Hitler vurulduysa oyun sonu koreografiden sonra. Sunucu/sözleşme değişmez |
| D13 | **Koltuk kamerası dönme sınırı** | **kodda düzeltildi** — 2026-09-12 (Claude, `packages/scene/src/prototype/model.ts` `lookLimits.yaw` 58° → 1,4 rad ≈ 80°; paylaşılan bakış sınırıyla aynı) | Kullanıcı 2026-09-12: odaklan modunda sağ/sol komşu botlara dönme sınırı yüzünden tıklanamıyor. Komşu koltuk 5–10 kişide 61–75° açıda; koltuk kamerası önce 58°'lik eski `constrainLook` ile kırpıyordu. Canlı doğrulama kullanıcıda |
| D14 | **Geliştirici senaryo modu (infaz vb. hızlı deneme)** | **teslim, kullanıcı doğrulaması bekliyor** — 2026-09-12 09:49–10:20 (Opus yan ajan). Rapor `docs/qa/claude/D14-dev-scenario.md`, kareler `docs/qa/claude/d14/` (9 jpeg). `pnpm -r typecheck` 6/6, `pnpm test` 669, web build geçti; dist'te `dev_scenario`/"Geliştirici" izi 0. Kullanım: oyun içinde `M` → Geliştirici → iki düğme. Commit YOK | Kullanıcı 2026-09-12: "silahı test edemiyorum, oraya gelene kadar; denemem için dev modu". Tasarım: game-core'da saf `devScenario(state, {scenario, actorId})` (senaryolar: `execution_now` → doğrudan infaz yetkisi yerel oyuncuda; `execution_round` → faşist sayacı infaz yuvasının bir öncesinde, deste üstü faşist, yerel oyuncu başkan), sözleşmede additive `dev_scenario` komutu, sunucuda yalnız `SECRET_TABLE_DEV_TOOLS=1` iken kabul (Vite API eklentisi yerelde ayarlar; Vercel'de ASLA), menüde yalnız `import.meta.env.DEV` iken "Geliştirici" bölümü (tembel chunk, üretim paketinde yok). Rol/deste gizliliği bozulmaz (yalnız sayaç, faz, deste sırası) |
| D15 | **Koltuktan tahtaya eğilme (birinci şahıs tahta bakışı)** | **teslim, kullanıcı doğrulaması bekliyor** — 2026-09-12 11:20–12:05 (Opus yan ajan). Rapor `docs/qa/claude/D15-lean.md`, kareler `docs/qa/claude/d15/` (9 jpeg). `pnpm -r typecheck` 6/6, `pnpm test` 717, web build geçti. Ölçüm 1920×1080: göz .51→1,375 m, eğim 48°, fov 43,9°, eğilmede çizim 181→133. Sözleşme/sunucu/game-core değişmedi; commit YOK | Kullanıcı 2026-09-12: "yukarıdan görmeye gerek yok; tuşa basınca kafamız ortadaki kartların üstüne giden bir geçişle gitsin, birinci açıdan çıkmadan kartların durumunu görelim". Koltuk modunda `Tahta` (B tuşu ve HUD düğmesi) artık tepeden kamera değil: göz koltuk tarafından tahtaların üstüne eğilir (yay biçimli 500 ms geçiş, kendi tarafından perspektif, iki tahta okunur), aynı tuş/Esc geri döner; eller gizlenir; bakış küçük açıda serbest. Genel bakış modundaki tepeden inceleme kalır |
| D16 | **El jestleri (emote): işaret, teslim, onay/ret, orta parmak, selam, alkış, yüz avuçlama** | **teslim, kullanıcı görsel kabulü bekliyor** — 2026-09-12, **tur 3** 2026-09-13 (rapor `docs/qa/claude/D16-emotes.md` §9, 29 kare `docs/qa/claude/d16/`; 3 tur; sözleşme 0.2.2 additive, sunucu/DB yok; boşta ek çizim 0, jest başına +3). Tur 3: kullanıcı "diğer jestler de işaret gibi bakışla dönsün" → `EmoteOverlay` ilk şahısta sekiz jesti göz/gövde ekseninde katı döndürüyor, kamu kolları da bakışa dönüyor (iki elli ±60°); `point` bire bir korundu, 1017 test | Kullanıcı 2026-09-12: "ellerimizle emote; diğerleri görsün; işaret parmağı bakışla dönsün; iki el teslim; orta parkmak; aklına gelenler". Bakış kanalına additive `emote` alanı (sunucu/DB yok), `G` **jest çarkı** (kullanıcı kararı: çip şeridi değil dairesel menü) + 1–8 / ok / fare / basılı tut-bırak, ilk şahıs `EmoteOverlay`, kamu `EmoteArms` (jest aktifken mount, boşta 0 çizim), 4 yeni kavrama (`point/openPalm/thumbUp/middleFinger`), etikette ikon, hız sınırı |
| D17 | **Yasama sırasında kamu elinde kart sırtları** | **teslim, kullanıcı görsel kabulü bekliyor** — 2026-09-12 (rapor `docs/qa/claude/D17-office-hands.md`, 9 kare `docs/qa/claude/d17/`; saf `officeHandState` + `OfficeHand`, 7 test; ölçüm: kamu eli olan koltukta **+4 çizim / +72 üçgen**, diğer koltuklarda 0; sözleşme/sunucu değişmedi) | Kullanıcı 2026-09-12: "kart seçen başkanın önünde 3 kart, sonra şansölyenin elinde 2 kart görünse akış daha güzel olur". `legislative_president` fazında başkan koltuğunda sağ el `holdFan3` + 3 kapalı sırt; `legislative_chancellor`/veto fazlarında şansölyede `holdFan2` + 2 sırt (yalnız KAMU bilgisi: faz + ofis; kart yüzü yok). Mevcut `cards_moved` cue'ları (deste→başkan, başkan→şansölye/atık, şansölye→tahta/atık) bu tutuşlardan başlayıp bu tutuşlarda biter; yerel oyuncunun kendi eli değişmez. Boşta ek çizim: yalnız o iki koltukta +1–2 |
| D18 | **Oynanış öncesi düzeltmeler (E2E + kural denetimi bulguları)** | **teslim, kullanıcı doğrulaması bekliyor** — 2026-09-12 (Opus yan ajan) | Kaynak: `docs/qa/claude/E2E-2026-09-12.md`, `docs/qa/claude/RULES-AUDIT-2026-09-12.md`. (1) Cue dağıtımı: hamle cue'ları yalnız hamleyi yapana gidiyor; diğer istemciler infaz/kart hareketi animasyonlarını görmüyor → son hamlenin olayları saklanır, `getView` istemcinin son sürümünden tam bir sonraki sürümdeyse alıcıya süzülmüş cue'ları döndürür. (2) Vurulan oyuncunun ekranı canlıda görünmedi → canlı tekrar + düzeltme (görünümden türeyen kalıcı katman). (3) Kural: aynı oyuncu bir oyunda iki kez incelenemez (başkan bağımsız). (4) Sunucu: `optionId` eksikse `INVALID_OPTION`, sessiz varsayılan yok. (5) Telefon jest çarkı dışarı dokununca kapanır; araç çubuğu sarması. Sonra canlı tekrar (2 tarayıcı + botlar). **TESLİM:** rapor `docs/qa/claude/D18-fixes.md`, kareler `docs/qa/claude/d18/` (15 jpeg + log). Migration GEREKMEDİ (olaylar `game_states.state` zarfında `__lastEvents`; `commit_move` imzası değişmedi). Sözleşme additive `sinceRevision` (`CONTRACT_VERSION` 0.2.0 SABİT, paket 0.2.4), `docs/CONTRACT.md §7.1`. GEÇTİ: `pnpm -r typecheck` 6/6, `pnpm test` **849** (26/55/107/332/74/255), web build. Canlı: 4 gerçek bağlam + 1 bot; kurban ve seyirci infaz cue'sunu aldı, VURULDUN kalıcı, telefon çarkı kapanıyor. Commit YOK |
| D19 | **"Nasıl oynanır" sekmesi + tüm kuralların canlı doğrulaması + dev senaryoları** | **kısmen teslim** — 2026-09-12 19:52–21:05 (Opus yan ajan). (a) "Nasıl oynanır" sekmesi TESLİM (`ui/howToPlay.ts` + `HowToPlayPanel.tsx`, tablolar `ROLE_SETUPS`/`BOARD_LAYOUTS`tan türer). (b) Dev senaryoları 2 → 9 TESLİM (`investigate_now`, `special_election_now`, `policy_peek_now`, `veto_round`, `veto_round_president`, `hitler_zone_round`, `chaos_round`; `SCENARIO_NEEDS_PLAYERS`; botRunner 9 bot + veto davranışı). (c) Canlı doğrulama KISMEN: **7 kişilik odada 27 GEÇTİ / 2 KALDI** (KALDI'lar betik kaynaklı yanlış negatif); 9 kişilik oda, canlı Hitler bölgesi/Hitler infazı, 5-6 kişilik deste tepesi ve "5 liberal zafer" KALDI. Rapor `docs/qa/claude/D19-rules-howto.md`, kareler `docs/qa/claude/d19/` (14 jpeg). `pnpm -r typecheck` 6/6, `pnpm test` **941**, web build geçti; dist'te dev izi 0. Commit YOK | Kullanıcı 2026-09-12: "bütün kurallar oynanabilir mi eksiksiz kontrol et; menüye nasıl oynanır sekmesi, kuralları şu olursa şuradan kullanırsın diye yaz". (a) Menüde "Nasıl oynanır" sekmesi: roller, tur akışı, oy, yasama, seçim sayacı/kaos, yetkiler (oyuncu sayısına göre tablo: sadakat incelemesi, özel seçim, deste tepesi, infaz) ve her birinin BU OYUNDA nereden/nasıl kullanıldığı (HUD seçenek çubuğu, rakamlar, Enter; inceleme sonucu elde zarf, deste tepesi elde 3 kart, infaz silah), veto (5 faşist kanundan sonra şansölye "veto öner", başkan kabul/ret; kabulde sayaç +1), Hitler bölgesi (3+ faşist kanunda Hitler şansölye seçilirse faşist zafer), zafer koşulları, elenen oyuncu, kontroller/jestler/kamera, telefon. (b) D14 dev senaryoları genişler: `investigate_now`, `special_election_now`, `policy_peek_now`, `veto_round`, `hitler_zone_round` — her kural botlarla hızlı denenebilir. (c) Canlı doğrulama: 7 ve 9 kişilik botlu odalarda her yetki + veto + Hitler zafer/yenilgi yolları, kareler `docs/qa/claude/d19/` |
| D20 | **Realtime sürüm sinyali gecikmesi** | **teslim, kullanıcı doğrulaması bekliyor** — 2026-09-12 19:52–20:50 (Opus yan ajan) | Teşhis: sinyal ASLINDA ulaşıyor (DB tetikleyici → `realtime.send(private)` → `room:<roomId>` özel kanalı uçtan uca çalışıyor, ölçüldü **363 ms**); D18'in "ulaşmadı" saptaması yanlıştı (WebSocket çerçeveleri ikili serileştiricide metin olarak arandı). Gerçek nedenler: 12 s'lik yedek yoklama + sinyal başına ARDIŞIK `lobby_view`+`view` iki HTTP turu. Düzeltme (istemci, **migration gerekmedi**): yedek yoklama 12 s → **3 s** + `poke()` ile sayaç sıfırlama (kota: 10 oyuncu × 20 istek/dk = 200/dk üst sınır), oyun içinde sinyal/yoklama için **hızlı yol** (yalnız `view`, lobi turu yok), kanal/olay adı sabitleri + 7 yeni test. Ölçüm: A tık → B görünüm ort. **2,5 s** / maks 2,97 s (yerel dev API köprüsü 3 bot altında baskın; üretimde yeniden ölçülmeli), yedek yoklama devreye girmedi. Rapor `docs/qa/claude/D20-realtime.md` |
| D23 | **İki dil: Türkçe + İngilizce, GitHub public yayın hazırlığı** | **teslim, kullanıcı doğrulaması bekliyor** — 2026-09-13 (Opus yan ajan). Rapor `docs/qa/claude/D23-i18n.md`, kareler `docs/qa/claude/d23/` (7 jpeg), betik `scratchpad/d23-shots.mjs`. Dil OYUNCUYA ait: sunucu/sözleşme/game-core dil bilmez, `CONTRACT_VERSION` 0.2.0 ve `contracts` paketi (0.2.6) DEĞİŞMEDİ, migration yok. Yeni: `apps/web/src/i18n/{index,tr,en}.ts` (397 anahtar; `tr` KAYNAK, `en` `satisfies` ile aynı küme), `ui/LanguageSwitch.tsx`, `ui/emoteText.ts`, `packages/scene/src/i18n/sceneText.ts` (+ `TableScene` additive `language?: 'tr'|'en'` propu, varsayılan `tr`). `usePrefs.language` (geri-uyumlu ayrıştırma, varsayılan `navigator.languages`), giriş/katıl/lobide sağ üst TR·EN, oyun içi M → Ayarlar; anında uygulanır. Tuval dokuları (tahta, kart, plaka) dil `draw` bağımlılığında → dil değişince yenilenir, eskisi dispose. Yayın: kök `LICENSE` (CC BY-NC-SA 4.0 tam metin) + `NOTICE.md` (Goat, Wolf & Cabbage atfı), README İngilizce özet + kurulum (Türkçe gövde korundu), Supabase proje kimliği docs'ta `<PROJECT_REF>`. typecheck 6/6 · **998 test** (+25) · build geçti, dist'te dev kodu yok. Canlı (bellek kipi 5202, 2 bağlam + 3 bot): TR ve EN oyuncu aynı odada aynı hamlede kendi dilinde metin ve tahta dokusu gördü; çizim çağrısı iki tarafta da aynı (masa 119, tahta 98). | Kullanıcı 2026-09-13: "İngilizce ekle, Türkçeyi silme, ikisini de kullanabileyim, GitHub public". Türkçe hiçbir metin silinmedi. Kalan: kullanıcı görsel kabulü, Vercel dev değişkenlerinin silinmesi, `docs/qa/**` görsellerinin kalıp kalmayacağı kararı |
| D24 | **Uzak kafa hareketinde ara değer (entity interpolation)** | **tur 2 teslim, kullanıcı doğrulaması bekliyor** — 2026-09-14 (Opus yan ajan). Tur 1 (2026-09-13) yayına gitti; kullanıcı gerçek Supabase'te "eller akıcı, kafalar hâlâ kasarak" dedi. Kök neden: sabit gecikme (medyan × 1,2 + 40 ms) geç paketi yakalamıyor, imleç son örnekte DURUYOR (tahmin yok) → bekle-koş. Tur 2 (yine yalnız ALICI; sözleşme/sunucu/game-core/migration ve `viewpointInterval` DEĞİŞMEDİ): gecikme = son 12 GELİŞ aralığının p90 + 60 ms (kayıp > %10 ise 150 ms), alt sınır medyan × 1,1, tavan `min(1500, staleAfter/2)`, artışta anında/azalışta τ = 2 s; kayıp geliş boşluğundan sezilir ve aralık slota bölünerek istatistiğe girer (şişmez); örnek damgası gönderim ızgarasına oturtulur (de-jitter, %15 çekme); son örnek geçilince son iki örneğin hızıyla τ = aralık/2 sönümle **en çok 1 aralık** ileri tahmin (reducedMotion'da yok, `frameloop="demand"` tahmin bitince durur); imleç hızı gerçek zamanın ±%20'si; geç paket sonrası yakalama doğal hızın 1,5 katı + 0,18 rad/s ile sınırlı (hedef sıçratılmaz). Ölçüm — sentetik (12 s, ideal 0,0083): düzenli p95 0,0102 → **0,0095**, maks 0,0204 → **0,0145**, sıçrama 2 → **0**; ±300 ms sapma + %10 kayıp p95 0,0211 → **0,0102**, maks 0,600 → **0,0128**, sıçrama 37 → **0**, donuk %10,3 → **%0,7**. Ölçüm — **GERÇEK Supabase Realtime** (5 oyuncu + 3 bot, 2 Playwright bağlamı, 20 s fare taraması, geliş aralığı p50 501 / p90 591 / maks 647 ms, kayıp 0): maks kare farkı 0,0445 → **0,0199**, > 20 mrad sıçrama 4 → **0**, donuk 2 → 1 (1081 kare). typecheck 6/6 · **1024 test** (+7) · web build geçti. | Kaynak: kullanıcı 2026-09-14. **Sapma:** kayıp `seq` boşluğundan değil geliş boşluğundan sezilir (tel `seq`i sahneye taşınmıyor); istenenin ötesinde ızgaraya oturtma + yakalama hız sınırı eklendi (onlarsız 0 sıçrama tutmuyordu); tahmin dinlenirken adımın %44'ü kadar ileride durabilir; hareketsizlikten ilk harekete geçişte tek seferlik yakalama kalıyor. **Kalan:** kullanıcının gerçek ağında doğrulama; commit/push yapılmadı |
| D21 | **Kod inceleme düzeltmeleri (A–K) + canlı yük düzeltmesi** | **teslim** — 2026-09-12 21:30–23:10 (Opus yan ajan). Rapor `docs/qa/claude/D21-review-fixes.md`, kareler `docs/qa/claude/d21/` (11 jpeg). **Migration GEREKMEDİ**; ajan commit atmadı, kullanıcı kodu oturum sürerken `dd5bc98` ile commit'ledi (rapor/kareler çalışma ağacında). Kullanıcı olayı: Vercel'de 7 kişiyle "sunucu kaldırmadı". Kök neden ölçüldü: her `view` isteği 14–17 ARDIŞIK Supabase turu (üye başına `getSession` + iki `listMembers` + iki `getGameState` + her istekte `touch_session` yazımı) × 3 s'lik yoklama × 7 oyuncu ≈ 2 000 sorgu/dk. Düzeltme: yoklama 3 s → **10 s** + lobide tamamen susar; `view` isteği **4 gateway çağrısı** (yeni additive `gateway.listSessions`, tek `listMembers`, `preload`lu `projectionMeta`, `touchSession` yalnız ≥30 s'de) → ölçüm 10 oyuncuda **17 çağrı/154,9 ms → 4 çağrı/37,0 ms**, oda başına tur **≈ %91 azaldı**. (A) ENGELLEYİCİ lobiye dönüş: `cancel_game` artık sürüm sinyali yayınlıyor (mevcut `notify_room_revision`, durum satırına dokunmadan) + `getView` yanıtına additive `roomStatus`; istemci `game_over`da ve `roomStatus='lobby'`de tam tura düşer (canlı bellek kipinde 6,6–8,1 s; gerçek Supabase'de sinyalle ≤4 s beklenir, ORADA ÖLÇÜLMEDİ). (B) eski `view` yanıtı artık görünümü geriye sarmıyor (sürüm koruması + istek nesli + istek boyunca birleştirme). (C) cue'lar elimizdekinden ileri HER sürümde gelir + additive `sinceGameId` (paket 0.2.6, `CONTRACT_VERSION` 0.2.0 SABİT). (D) `resync` bayrağı tüketilmiyor. (E) şansölyeye iki sırt uçuyor (koltuk başına cue seçimi, yeni saf `pickOfficeHandCue` + fixture `legislative-chancellor-seat-two-cues`; kare t250/t400, ek çizim 0). (F) menü açılışta Ayarlar sekmesinde. (G) jest çarkı zemin/"Geri" click yedeği + `Escape` kancada. (H) bot oturumları `localStorage`'da yeniden kullanılır, 429'da anlaşılır hata ve durur. (I) küçükler: `ACTIONS_REQUIRING_OPTION` kaldırıldı (kural sunucuda, seçenekli her eylemde `optionId` zorunlu), yorum düzeltmeleri, `errorLine` ham kod, `shotFresh` kalıcı dalda false. (K) `dev_scenario` yalnız host (UI'da da devre dışı). GEÇTİ: `pnpm -r typecheck` 6/6, `pnpm test` **973** (D21 öncesi 941; +32 test), web build; canlı 2 bağlam + 3 bot ile ~110 s oyun (25 insan hamlesi, infaza kadar ilerledi, hata yok). | Kaynak: kullanıcı/koordinatör kod inceleme turu + canlı yük olayı. **Kalan:** üretime deploy (Vercel paketi hâlâ eski davranıyor), gerçek Supabase'de lobiye dönüş gecikmesi ölçümü, `view` için "sürüm değişmedi" kısa yanıtı |
| D25 | **Depo yüzeyini İngilizceleştirme (public GitHub)** | **teslim, kullanıcı doğrulaması bekliyor** — 2026-09-18 (Opus yan ajan). Kod davranışı değişmedi; sözleşme/sunucu/game-core/scene/migration dokunulmadı. Yeni: `docs/SETUP.md` (İngilizce kendi Supabase/Vercel kurulumun; anonim giriş + 30/saat/IP notu, migration 0001–0008 CLI ve Management API yolu, env tablosu, Vercel ayarları, güvenlik modeli, Free katman kotası, sorun giderme), `docs/ARCHITECTURE.md` (forum/HN okuru için: paket haritası, hamlenin uçtan uca yolu, CAS, belirlenimcilik + 1 800 tohumlu simülasyon, projeksiyon, sahne hattı, canlı bakış kanalı, i18n modeli, test sayıları), `docs/README.md` (belge dizini: hangisi İngilizce, hangisi Türkçe çalışma notu), `CONTRIBUTING.md`. Değişen: `.env.example` (tamamen İngilizce), `docs/ASSETS.md` (yerinde İngilizce, "Asset inventory"; tablolar + font lisansları korundu), `README.md` (test rozeti 1017 → **1024**, SETUP/ARCHITECTURE/docs dizini bağlantıları, Contributing bölümü; Türkçe özet ve lisans bölümü korundu), `apps/web/index.html` (`lang="en"` statik varsayılan, İngilizce description + OG/Twitter meta; `og:image` yok), kök `package.json` (description/license/repository/keywords), `CLAUDE.md` + `AGENTS.md` (tek satır İngilizce not). **Kod içi Türkçe yorumlar kaldı** (kullanıcı kararı); Türkçe belgeler silinmedi. GEÇTİ: `pnpm -r typecheck` 6/6 · `pnpm test` **1024** · web build. Dil doğrulaması: `tr-TR` → Türkçe UI + `lang="tr"`, `en-US` → İngilizce UI + `lang="en"`. Commit YOK | Kullanıcı 2026-09-18: proje yabancı forumlarda (three.js, HN, Reddit) paylaşılacak; README'den linklenen her şey İngilizce olsun. **Kalan:** kullanıcı doğrulaması ve public depoya sürüm (`scripts/publish-public.sh`) |
| D28 | **Varsayılan dil İngilizce** | **teslim** — 2026-09-18. Depo/ön izleme herkese açık olduğu için uygulama HER ziyaretçide İngilizce açılır; `navigator.languages` artık okunmaz (`detectLanguage` kaldırıldı, yeni `DEFAULT_LANGUAGE='en'`). Türkçe giriş ekranındaki TR/EN anahtarından ya da menüden seçilir, `usePrefs` ile kalıcıdır. `usePrefs` kayıt yoksa `currentLanguage()` alır; test kurulumu (`apps/web/test/setup.ts`) navigator taklidi yerine `setLanguage('tr')` ile kaynak dili sabitler. Değişen: `apps/web/src/i18n/index.ts`, `ui/usePrefs.ts`, `main.tsx`, `test/setup.ts`, `i18n/i18n.test.ts`, `ui/usePrefs.language.test.ts`, `README.md`, `docs/{ARCHITECTURE,DEPLOYMENT,design/D23-i18n}.md`. GEÇTİ: typecheck 6/6, **1033** test, build; canlı (bellek kipi 5209, Playwright): `tr-TR` ve `de-DE` tarayıcılar kayıt yokken İngilizce (`lang=en`), TR'ye basınca Türkçe ve yenilemede kalıcı. | Kullanıcı 2026-09-18: "temel sürüm full default İngilizce olsun, isterlerse Türkçe" |
| D27 | **README tanıtım GIF'i (10–12 s)** | **teslim, kullanıcı görsel kabulü bekliyor** — 2026-09-18 (Opus yan ajan). Yalnız README + `docs/media/`; **kod değişmedi**. `docs/media/demo.gif` (800×450, 12 fps, 10,75 s, 129 kare, 5,73 MB) ve `docs/media/demo-poster.jpg` (71 KB). Dört sahne, İngilizce HUD, 7 kişilik bot masası: (1) koltuktan kafa gezdirme 3,04 s, (2) jest çarkı → "Hands up" 2,60 s, (3) `B` ile tahtalara eğilme 1,68 s, (4) infaz yetkisi: hedef seç + "Execute" 3,20 s. Çekim ffmpeg'siz: Playwright + CDP `Page.startScreencast` (jpeg q92, zaman damgalı kareler) → PIL 11.3 ile tek ortak palet + kare farkı (`scratchpad/d27-record.mjs`, `scratchpad/d27-gif.py`). README: rozetlerin altındaki kapak GIF oldu, `hero-table.jpg` "Screenshots" başına taşındı, `PREVIEW_URL_HERE` aynen duruyor. GEÇTİ: `pnpm --filter @secret-table/web build`. Commit YOK **Tur 3 (2026-09-18):** ilk-atış donması depoda çözüldü (`GunProp.tsx` patlama malzemelerini silah ele geldiğinde opaklık 0 ile ön ısıtıyor). Ölçüm (ısıtma atışı olmadan, oyunun ilk atışı): ateş anındaki kare boşluğu **533 ms → 19 ms**, ateş±300 ms penceresindeki kare sayısı **2 → 36**; kalan boşluklar yalnız komut öncesi/sonrası boştaki karelerde. Betikteki geçici "ısıtma infazı" varsayılan KAPALI (`WARMUP=1` ile geri açılır). İnfaz segmentinde duyuru katmanı `page.addStyleTag` ile gizlenip ateşten hemen sonra geri açılıyor: hedef seçimi ve alev karesi şeritsiz, sonda yalnız "EXECUTION — … was shot". Yeni GIF **5,51 MB**, 10,67 s, 128 kare | **Tur 2 (2026-09-18):** tur 1'de bulunan üç kusur depoda düzeltildikten sonra GIF yamasız yeniden çekildi — `docs/media/demo.gif` 800×450, 12 fps, **10,67 s**, 128 kare, **5,56 MB** (bakış 2,86 s · jest 2,70 s · tahta 1,64 s · infaz 3,32 s). Canlı doğrulandı: birinci şahısta silah kalkıyor, nişan alıyor, **namlu alevi + kıvılcım + duman** görünüyor, koreografi bot hamlesine rağmen 2 600 ms tam oynuyor (`scratchpad/d27-check/fire-live.png`); jest de yamasız oynuyor. Betik eklemeleri: kayıt dışı "ısıtma infazı" (patlama malzemeleri ilk çizimde ~530 ms kare durduruyor, screencast o pencerede kare vermiyordu) ve `d27-gif.py` `<SEGMENT>_KEYS` ile 120 ms'lik flaşın 12 fps ızgarasına zorla oturtulması | **Tur 1'de bulunan, tur 2'de depoda düzeltilen üç kusur:** (a) bellek kipinde (Supabase istemcisi yok) `viewpointChannel.sendEmote` hep `false` döndüğü için YEREL jest hiç oynamıyor — GIF için yalnız tarayıcıya servis edilen modülde geçici yama yapıldı; (b) canlı infazda başkanlık aynı sürümde sonraki oyuncuya geçtiği için D12 birinci şahıs koreografisi (silah kalkışı, namlu alevi) HİÇ oynamıyor: silah bir karede kayboluyor, yalnız `player_eliminated` cue'su + duyuru görünüyor. (c) `controlScheme.ts:389` İngilizce HUD'da sabit Türkçe `'Onayla'` gösteriyor |
| D29 | **"Nasıl oynanır" lobide, düğmeyle açılan görsel modal** | **teslim** — 2026-09-18 (Opus yan ajan). Rapor `docs/qa/claude/D29-howto-modal.md`, 8 kare `docs/qa/claude/d29/`. Oda kurulduktan SONRA lobinin sağ sütununda tam genişlikte **"Nasıl oynanır" / "How to play"** düğmesi; tıklayınca tam ekran modal (`role="dialog"` + `aria-modal`, odak tuzağı, açılınca odak başlığa / kapanınca düğmeye, `Escape` + zemin + "Kapat"). Giriş ekranındaki üç adımlık şerit KALDI (o "bu sayfada ne yapacağım"ı söyler), altına aynı düğme eklendi — D26 solo lobiyi atladığı için kılavuza tek erişim noktası orası. Oyun içi menüdeki (`M`) sekme DURUYOR ve **aynı bileşeni** çiziyor (tek kaynak). D19 içeriği (roller/yetki tabloları, tuşlar, jestler) korundu, üstüne bölüm başına bir görsel geldi. Yeni `ui/howToPlayArt.tsx`: yalnız **satır içi SVG**, dış dosya yok — politika ve rol kartları + JA/NEIN pusulaları (`materials/cardArt.ts`'in `drawCard()` yerleşimi ve amblemleri SVG'ye çevrildi), kart arkası, **iki tahta** (liberal 5 / faşist 6 yuva) yetki ikonları + VETO rozeti + Hitler bölgesiyle, seçim sayacı, kimlik zarfı, koltuk plakaları. **Tek kaynak:** `BOARD_ICONS` yol verileri, palet ve yuva planı yeniden kullanıldı; bunun için `three` İÇERMEYEN yeni alt yol `@secret-table/scene/art` (yeni `packages/scene/src/art.ts` + `exports["./art"]`) açıldı, yuva→yetki eşlemesi `boardSlotArt()` ile `BOARD_LAYOUTS`tan türer. Bilerek iki sapma: tahta 360 px'te okunsun diye YENİDEN yerleştirildi (bant yazısı yerine ikon + altta HTML açıklama listesi) ve yuva ikonları hayalet ton yerine parti renginde. 45 yeni i18n anahtarı (tr+en, açık `Record<…, TextKey>` haritaları; Türkçe metin silinmedi); kartın ÜZERİNE basılı büyük harf dizgiler `sceneText`ten okunuyor (3D kartla aynı dizgi). İçerik hem modalde hem menü sekmesinde **tembel**: giriş paketi 396 994 B → **399 590 B (+2,6 kB)**, CSS 35,29 → 39,43 kB, ilk yükleme toplam **+1,75 kB gzip**; `HowToPlayPanel` 23,13 kB (gzip 6,45) ayrı chunk, sahne chunk'ı ortak parçalar ayrıldığı için 172,19 → 163,11 kB'a indi. Solo kipte düğmeye basılmazsa **ek istek yok** (düğmeye gelince ön indirme). Not: modal `createPortal` ile `document.body`ye taşındı — lobinin kartı `card-rise` animasyonunu `fill: both` tuttuğu için `transform` kalıyor ve `position: fixed` kapsayan bloğu oluyordu (ölçüldü: panel `top: -194px`, başlık görünmüyordu). DEĞİŞMEYEN: sözleşme, `packages/server`, `game-core`, migration, `apps/web/api/*`, README ön izleme linki. GEÇTİ: typecheck 6/6, **1066** test (önce 1046, +20), web build. **Canlı (bellek kipi 5211, Playwright):** masaüstü 1280×720 ve telefon 390×844 × `tr-TR`/`en-US` dört koşu; konsol/sayfa hatası **0**, `Escape` kapattı ve odak düğmeye döndü. Bilinen küçük eksik: zeminin üstünde tekerlek arkadaki lobiyi kaydırıyor (kaydırma kabı `.shell`, `body` değil). Commit YOK | Kullanıcı 2026-09-18: "nasıl oynanır kısmı lobi kurulduktan sonra butonla bence modal olarak açılsın ve kart resimleriyle falan açıklayıcı şekilde anlatılsın onu oraya taşıyalım" |
| D26 | **Tarayıcıda "tek başına dene" (solo demo, botlarla, sunucusuz)** | **teslim** — 2026-09-18 (Opus yan ajan). Rapor `docs/qa/claude/D26-solo.md`, 7 kare `docs/qa/claude/d26/`. Giriş ekranında ikincil **"Try it solo" / "Tek başına dene"**: isim sorulmadan 1 insan + 5 bot ile masa kurulur ve oyun HEMEN başlar. Oyunun tamamı tarayıcıda: `GameService` + `InMemoryGateway` tembel yüklenir (`packages/server` SAF olduğu için değişmeden koşuyor), `solo/soloTransport.ts` `apps/web/api/game.ts`'in yerel ikizidir (aynı `action`/`payload`, aynı `ApiResult`, aynı durum kodları), `apiClient` tek anahtarla (`setSoloTransport`) oraya düşer ve DIŞ YÜZEYİ değişmez. Supabase yok (sabit `solo:<id>` token, `ensureGuestSession` çağrılmaz), Realtime/bakış kanalı yok (yerel sürüm sinyali + yerel jest), heartbeat yok, kalıcılık yok. Bot KARAR mantığı `dev/botRunner.ts`'ten saf `solo/botBrain.ts`'e çıkarıldı (dev botların davranışı aynı); solo botlar 400–1200 ms rastgele bekler ve yalnız kendi sırasında oynar. Köşe rozeti: "Solo demo — bots, nothing is saved" + "Play with friends". **Üretimde AÇIK, `DEV_TOOLS` kapısına bağlı değil**; maliyeti sıfır: kod tembel (`startSolo` 7,6 kB + sunucu/motor 43,4 kB ayrı chunk), giriş paketi 394,153 B → 396,994 B (**+2,8 kB, %0,7**). DEĞİŞMEYEN: sözleşme, `packages/server`, `game-core`, migration, `apps/web/api/*`. GEÇTİ: typecheck 6/6, **1046** test (önce 1033, +13), build. **Canlı (bayraksız üretim derlemesi, 5210, Playwright):** `/api/game` isteği **0**, `supabase.co` isteği **0**, konsol/sayfa hatası 0; rol dağıtımı → aday → oylama → kanun → başkanlık yetkisi → oyun sonu. Bilinen sapma: `dev_scenario` dizgesi giriş paketinde 0 ama TEMBEL sunucu chunk'ında 2 kez geçiyor (motorun kendi `switch`'i; tarayıcıda `process.env` boş nesneye katlandığı için daima `NOT_ALLOWED`). Commit YOK | Kullanıcı 2026-09-18: ön izleme linkine tek başına gelen ziyaretçi oyunu görebilsin; bot modu üretime açılmasın (anonim hesap kotası + 6 kat sunucu yükü) |
| D8 | **Site / oyun kur / lobi: modern, güncel tasarım** | açık | Hem eski hâl hem B6 (2026-09-10) kullanıcı tarafından beğenilmedi ("dandik, özensiz"). Kod yazmadan önce: (1) 3–4 referans oyun lobisi ve modern web oyunu giriş ekranı toplanır, (2) tasarım taslağı hazırlanır (design canvas: giriş, katıl, lobi × masaüstü/telefon), (3) kullanıcı onayı, (4) sonra kod. Yön: koyu, sinematik arka plan (sahneden alınmış gerçek render veya stilize illüstrasyon), güçlü başlık tipografisi (yerel serif alt kümesi kabul), tek odaklı büyük eylem, lobide oyuncu avatarları kartlar hâlinde, canlı durum animasyonları; telefon öncelikli |

2026-09-11 kararı: **şimdilik kod yazılmaz, planlar netleşir.** **GPT/Codex tarafı kapatıldı:** tasarım, model ve kod dahil her iş Claude'da (ilk sahne zaten GPT üretimiydi; istenirse GPT'ye yalnız review attırılır, zorunlu değil). Kod yasağı kalkınca sıra: D1 el üretimi (dev sayfasında yakın plan kanıtı) → D8 tasarım taslağı. Sonraki: kullanıcı yeni maddeleri ekledikçe bu tabloya girilir. A5 gerçek kabul ve C4 instancing bu
listenin arasına gerektiğinde alınır.

## B. Sonra — görsel ve sosyal özellikler (A5'ten sonra)

> Not: D listesi B'nin önüne geçti; B2/B3/B4 sırasıyla D6/D3/D3 içinde ele alınır.

| # | İş | Not |
| --- | --- | --- |
| B1 | Bilgi metinleri | A2'deki şerit temel; ek olarak olay bildirimleri ve kısa yardım ipuçları |
| B2 | İsim kartları yeniden tasarım | Şu an koltuğa dönük duruyor; kameraya bakan (billboard) veya sandalye arkası/koltuk üstü etiket alternatifi (D6) |
| B3 | Karakter tipleri seçilebilir | **Yapıldı 2026-09-12 (D3.4)** — `room_members.avatar_character`/`avatar_skin` (0008 additive, **uygulama kullanıcıda**), `PlayerView.avatar` + `LobbyMember.avatar` (her zaman dolu; seçilmemişse koltuk varsayılanı), `set_avatar` lobi komutu (yalnız lobide, idempotent, geçersiz kimlik `INVALID_OPTION`), mevcut lobide `AvatarPicker` — D8 yeniden tasarımı beklenmedi. Aynı karakteri iki oyuncu seçebilir; benzersizlik zorunlu değil. Rapor `docs/qa/claude/D3-characters.md` |
| B4 | Daha büyük ve iyileştirilmiş karakterler | D3 içinde; Claude yapar |
| B5 | El hareketleri / emote | El kaldır ve birkaç emote; baş yönü kanalı gibi geçici Realtime mesajı, oyun durumuna yazılmaz, hız sınırlı; sahne animasyonu |
| B6 | Giriş ve lobi arayüzü | **Yapıldı 2026-09-10** (`docs/qa/claude/B6-entry-lobby.md`, görseller `docs/qa/claude/b6/`; kullanıcı görsel kabulü bekliyor). Tasarım yönü: oyunun paletinde (ceviz, koyu yeşil keçe, krem kart, pirinç) tam ekran giriş; three yüklenmez (giriş paketi hafif kalır), arka plan CSS/SVG masa ve kart motifi. Giriş: büyük başlık + tek satır tanıtım, isim alanı, iki net eylem ("Oda aç", "Davet koduyla katıl"), "Nasıl oynanır" 3 adımlık şerit, altbilgi (sürüm/istemci). Katıl sayfası aynı dil, kod dolu. Lobi: koltuklar yuvarlak masa çizimi üzerinde (5–10), davet bağlantısı kopyala + `navigator.share`, büyük "Hazırım" anahtarı, host için "Oyunu başlat" ve eksik oyuncu sayacı, bağlantı/hazır rozetleri, dev bot düğmesi korunur; karakter seçimi için yer (B3'te dolacak). Telefon öncelikli, klavye erişilebilir, Türkçe. Testler: mevcut Entry/Join/Lobby testleri + yeni render testleri |

## C. Performans (ölçüm önce)

1. **Yapıldı (Mac, 2026-09-10):** `docs/qa/claude/PERF-C1.md` — M2'de vsync tavanı; CPU 4× yavaşta standard koltuk 33 FPS; darboğaz her karede React yeniden render (bakış state'i). Telefon ölçümü kullanıcıda.
2. **Yapıldı 2026-09-10:** sahne + three ayrı chunk, lobide ön-yükleme; ilk yükleme JS
   1.568 → 577 kB (gzip 443 → 172 kB). Font alt kümesi yapılmadı (fontTools yok);
   fontlar artık yalnız sahne chunk'ıyla iniyor. Rapor `docs/qa/claude/C2-bundle-mobile.md`.
3. **C3 yapıldı (2026-09-10):** bakış ref+useFrame (`lookChannel.ts`), baş örnekleri `PeerHeads`, gölge 1024, otomatik kalite kademesi. CPU 4× standard koltuk 31→60 FPS, React render 674→0, uzun görev 0. Rapor `docs/qa/claude/C3-perf.md`.
4. Tekrarlayan nesnelerde instancing; çizim çağrısı hedefi ≤120 standard (şu an 223–235; genel masa kamera uçuşu 28–38 FPS CPU 4×'te, sıradaki perf işi).
5. Kalite kademesi cihazdan otomatik.
6. R3F'ten çıkış yalnız ölçüm React tarafını gösterirse ve yalnız sıcak parçalar için.

## D. Çalışma biçimi

- Ana sohbet ve koordinasyon Claude. Kod işleri gerekirse Opus 5 yan oturumlarına
  verilir; gizli değer, migration ve canlı doğrulama ana oturumda kalır.
- GPT/Codex tarafı 2026-09-11'de kapatıldı: görsel tasarım ve model üretimi dahil her
  iş Claude'da. Dış araç (Blender) yok; modeller kodda prosedürel üretilir. Kullanıcı
  isterse hazır bir teslim GPT'ye review'a verilir; plan buna bağlı değil.
- Git push ve Vercel deploy kullanıcıda. Ücretli plan yok. DB toplu temizliği yok.
