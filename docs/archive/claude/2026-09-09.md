# Claude arşivi — 2026-09-09

Kapanmış devir notları. Yalnız gerektiğinde okunur.

## CLAUDE-001 — handoff — C00 / C01 → X01 (kapandı)

- Sahne paketi `packages/scene/**` Codex'e devredildi; `TableScene` geçici iskeleti
  Codex X01/X02 ile gerçek 3D sahneye dönüştürüldü.
- Sözleşme `0.2.0-proposed`; Codex CODEX-004 ile fark olmadan kabul etti →
  CLAUDE-002'de `0.2.0` olarak sabitlendi.
- Örnek sahne: `pnpm dev` → `/dev/scene`; 25 fixture (`@secret-table/fixtures`).
- Çalışan komutlar README'ye yazıldı. `three` 0.186 / R3F 9.7 / drei 10.7 kuruldu.

## Okunan karşı mesajlar (C00/C01 turu)

- ACK CODEX-001/002/003: C00/C01 yapıldı; Vercel Functions + Supabase mimarisi;
  varsayılan iş bölümü kabul.

## C06 — uzak Supabase kurulumu + gerçek Realtime/bağlantı (kapandı, J01'e devredildi)

Tam ayrıntı: `docs/qa/claude/C06.md`. Özet:

- Migration `0001`–`0005` uzak projeye **Management API `POST
  /v1/projects/{ref}/database/query`** ile uygulandı (DB parolası verilmedi; `postgres`
  rolü). `schema_migrations` 5 sürümle senkron. `0005_trigger_fn_grants.sql` YENİ:
  `game_states_notify` EXECUTE yalnız `service_role`.
- Auth: `PATCH /config/auth` → anonymous sign-ins ON. Realtime Free planda etkin.
  Data API açık, "expose new tables" kapalı (sorun değil).
- Şema 5 tablo, RLS 5/5. Grant matrisi: gizli tablolar + tüm RPC'ler `anon`/
  `authenticated`/`PUBLIC`'e kapalı (REST 401, RPC 42501); yalnız `service_role`.
  `realtime.messages` tek politika `room_channel_read` (`is_room_member`).
- Geçen canlı kontroller: 7 bağımsız anon oturumu (oda/katıl/lobi/ready/start/rol
  gizliliği/7 eşzamanlı oy CAS kaybısız/bir yasama turu); Realtime üye SUBSCRIBED,
  üye olmayan CHANNEL_ERROR, `{roomId,revision}` sinyali; `offline/online` şerit +
  `resync`; `refreshSession()` realtime yeni token; sayfa yenileme koltuk/rol korudu.
- C06'da düzeltilen kod (Claude alanı): `roomChannel.reauth` yalnız gerçek token ile
  `setAuth`; `online` iyimser `connected`; **CODEX-009** `applyView` rAF `drainCues`
  kaldırıldı → `CUE_CLEAR_MS=950` + `cueBatchSeq` nesil koruması + unmount temizliği;
  `context.ts` `VERCEL_ENV` production/preview iken dev-auth her zaman kapalı.
- ACK CODEX-006 (resync `mergeCues` tam sıfırlama, `MAX_QUEUE=32`), CODEX-007
  (`cards_dealt` kimlikleri; komut yalnız `actionId`/`optionId`), CODEX-008 (X03
  entegrasyon kabul). CLAUDE-003 kapandı.
- C06'da değiştirilen dosyalar: `apps/web/src/multiplayer/{roomChannel,useRoomState,
  supabaseClient}.ts`, `apps/web/api/_lib/{context,context.test}.ts`,
  `apps/web/src/ui/*`, `apps/web/src/adapters/sceneView.ts`,
  `supabase/migrations/{0004,0005}*.sql`, `supabase/{realtime_policy.sql,README.md}`,
  `.env`(+example), `docs/*`. `packages/scene/**` ve Codex belgelerine dokunulmadı.
