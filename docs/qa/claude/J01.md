# J01 QA — yerel uygulama + GERÇEK uzak Supabase ile tam oyun doğrulaması

Tarih: 2026-09-09. Sahip: Claude. Ortam: yerelde `pnpm dev` (Vite API köprüsü,
`mode=supabase`) + uzak Supabase proje `<PROJECT_REF>` (eu-central-1, Free plan).
Sözleşme `0.2.0` sabit. Kod sürümü: `git` hâlâ commit'siz; `apps/web/**` C06 sonrası
değişmedi (bu oturumda J01 için kod değişikliği yapılmadı). **Vercel deploy YAPILMADI**
(kullanıcı kararıyla ertelendi). Ücretli plan/hizmet açılmadı.

Bu rapor iki bölüm: **A. Otomatik oyuncu (bot) testleri** ve **B. Gerçek tarayıcı
istemcisi**. Gerçek çok kişili insan oyun gecesi (J01'in insan bileşeni) **YAPILMADI** —
bkz. § E.

## Baz kontrol (bu oturum)

- `pnpm install --frozen-lockfile` temiz · `pnpm typecheck` 6/6 · `pnpm test` başta **163**,
  oturum sonunda **183** (fark: Codex paralelde `packages/scene/src/prototype/**` altına
  `prototype.test.ts` +20 test ekledi — Codex sahiplik alanı, benim yollarımı etkilemez).
- Çalışan sunucu: `:5173` bu oturumun `pnpm dev`'i. Codex prototip sunucusu ayrı `:5174`.

---

## A. Otomatik oyuncu (bot) testleri — GERÇEKTEN çalıştırıldı

Sürücü: `scratchpad/j01.mjs` (oturuma özel; her oyuncu ayrı `POST
$VITE_SUPABASE_URL/auth/v1/signup` ile GERÇEK anon `access_token` alır, sonra `Bearer`
ile yerel `/api/game`). Motor tüm fazlardan geçirilir; seçilen seçenek "kurala uygun ama
kör" (roller gizli kaldığı için bot strateji gütmez).

### A1 — 7 bağımsız oturum, tam oyun → `game_over`

- Oda `d8e11274-…`, 7 farklı gerçek `auth.users`, 7 farklı `playerId`. `mode=supabase`
  (dev düşüşü yok). Süre ~261 sn (eu-central RTT), 30 tur.
- **Sonuç:** `game_over` `winner=liberal` `reason=hitler_executed`. Board L2 / F4.
  `revision=79`, `processed_commands=79` (birebir; boşluk/çift yok).
- **Geçen kontroller (159 assert):**
  - role_reveal: her oyuncu yalnız kendi rolünü gördü; herkese açık `players[]`'de
    `role`/`party`/`vote` alanı yok. Rol dağılımı 7p doğru (2 fascist + 1 hitler).
  - 7 EŞZAMANLI `ack_role` → 7/7 kabul (aynı `phaseId`).
  - 7 EŞZAMANLI oy → 7/7 kabul, CAS kaybı yok; `lastElection` 7 oy içeriyor.
  - Bilerek BAŞARISIZ seçim (hepsi hayır) → `outcome=rejected`, `electionTracker` +1,
    yeniden adaylık.
  - Yasama: `president_discard` (el 3 kart, yalnız başkanda) → `chancellor_choice`
    (el 2 kart, yalnız şansölyede; başka oyuncu `privateView.hand` boş) → enact.
  - Yürütme yetkileri: **`investigate_loyalty`** (+`ack_private_result`; gizli inceleme
    sonucu başka oyuncuya sızmadı), **`call_special_election`**, **`execution`**
    (Hitler öldü → liberal galibiyet).
  - Tekrarlanan komut (aynı `commandId` 2. kez) → `ok:true`, `revision` değişmedi
    (idempotent); `lastElection`'da oyuncu başına tek oy.
  - Token yenileme (GoTrue `grant_type=refresh_token`) oyun ortasında: yeni
    `access_token`, sonraki çağrılar başarılı, aynı `playerId` + aynı rol.
  - Kaçan bildirim / resync: oyunu birkaç tur "izlemeyen" oyuncu `view?resync=true` →
    aynı `gameId`/`roomId`, rol korunmuş.
  - `game_over` 7 rolü açtı; her açılan rol başlangıç rolüyle birebir.

### A2 — 6 bağımsız oturum, tam oyun → `game_over`

- Oda `a5dd370e-…`, 6 farklı gerçek `auth.users` + 6 farklı `playerId`. Süre ~264 sn,
  36 tur.
- **Sonuç:** `game_over` `winner=fascist` `reason=fascist_policies_enacted` (F6).
  Board L1 / F6. `revision=77`, `processed_commands=77` (birebir).
- **Geçen kontroller (161 assert):** A1'deki tüm başlıklar + 6p'ye özgü:
  - Yürütme yetkisi **`policy_peek`** (+`ack_private_result`), **`execution`** ×2.
  - **Veto akışı:** fascist=5'te şansölye `request_veto` → başkan `respond_veto`
    (**reject**) → şansölye yine `enact_policy`. (Not: `respond_veto` **accept** dalı
    bu koşuda tetiklenmedi; game-core birim testlerinde var.)
  - 6 EŞZAMANLI oy → 6/6 kabul, CAS kaybı yok.
  - Rol dağılımı 6p doğru (1 fascist + 1 hitler).

### A3 — DB bütünlüğü (Management API SQL ile doğrulandı)

- Her iki oyunda `game_states.state->phase->kind = 'game_over'`, doğru `winner`.
- `revision == processed_commands` (79/79, 77/77): kabul edilen her durum-değiştiren
  komut tam bir ledger satırı bıraktı, sürümü tam +1 artırdı — çift/boşluk yok.
- `rooms.status` `game_over`'da `in_game` kalıyor (tasarım: durum yalnız
  lobby→in_game geçişinde yazılıyor; motor fazı DB `state` içinde `game_over`). Hata değil.

---

## B. Gerçek tarayıcı istemcisi — GERÇEKTEN oynandı

Kurulum: `scratchpad/assist.mjs` 5 gerçek anon bot + host; davet kodu; Claude tarayıcıda
6. oyuncu (ayrı gerçek anon kimlik). Bot host başlatır, botlar kendi sıralarını oynar,
tarayıcı koltuğu atlanır. Tarayıcı: in-app Browser (`localhost:5173`).

### B1 — tamamlanan oyun (oda `3bf3dfb4-…`)

- 6 üye, Claude = gerçek tarayıcı istemcisi (rol: `liberal`). 
- **Sonuç:** `game_over` `winner=fascist` `reason=hitler_elected_chancellor`
  (`revision=60`). Bir bot şansölyeye kör şekilde Hitler'i (Bot-Bora) F3'te aday
  gösterdi → anında fascist galibiyet.
- **Gözle doğrulanan:**
  - 3D sahne gerçek oyun rotasında render oldu (yuvarlak masa, 6 avatar, Liberal/Fascist
    tahtaları, deste/atık, koltuk kartları, lambalar). WebGL 900×845 canvas, konsol
    hatası yok.
  - Özel bilgi doğru ve sızmıyor: "Rolün: Liberal"; ilk (iptal edilen) oyunda "Rolün:
    Faşist — Biliyorsun: Bot-Efe Hitler" (6p'de fascist Hitler'i tanır). Herkese açık
    `Oyuncular` panelinde rol/parti yok.
  - Cue animasyonları oynadı ve **tamamlandıktan sonra kuyruk temizlendi** (hayalet
    kalıntı yok): oy kartları belirdi→çevrildi→kalktı; başkana 3 kart dağıtıldı (yalnız
    tarayıcıda yüzü açık); Fascist/Liberal tahta karosu yerleşti; bir oyuncu elendi
    (avatar çöktü, kalıcı); `game_ended` → kazanan bandı + tüm roller açıldı.
  - Başarısız seçim gerçek istemcide: 2–2 ve 3–3 beraberlik → `ret`, tracker arttı,
    yeniden adaylık.
  - `requiresConfirmation` UX: nominate/vote/discard "seç → Gönder/Vazgeç"; Gönder'siz
    komut gitmiyor.
  - **Malformed komut reddi:** test-koşum betiğinin bir hatası eşleşmeyen
    `actionId`/`optionId` gönderdi → sunucu `INVALID_OPTION` (retryable:false) döndü,
    durum değişmedi. (Betik hatası; uygulamanın savunması doğru çalıştı.)

### B2 — CODEX-009 canlı animasyon kontrolü (kapanış)

- Kod tarafı (C06'da yapıldı) yerinde doğrulandı: `useRoomState.applyView`'da rAF
  `drainCues` yok; `CUE_CLEAR_MS = 950` sonrası nesil korumalı (`cueBatchSeq`) timer;
  `resync`/`gameId` değişimi → `mergeCues` kuyruğu hemen boşaltıyor + bekleyen timer
  iptal; unmount temizliği. Sahne tarafı süreler sabit
  (`policy_enacted 620`, `votes_revealed 480`, `cards_dealt 520`, `game_ended 850`,
  `player_eliminated 350` ms), `reducedMotion` → 100 ms.
- **Gözle:** animasyonlar gerçek oyun rotasında görünür şekilde BAŞLADI ve TAMAMLANDI
  (eski rAF-drain hatasındaki gibi anında kesilmedi); bitince kalıntı bırakmadan
  temizlendi.
- **Resync sırasında iptal:** cue penceresi içinde/hemen sonrasında "Yenile" (resync)
  arka arkaya basıldı → sahnede hayalet oy kartı / yarım animasyon kalmadı; görünüm
  yetkili duruma yakınsadı; konsol hatası yok.
- **Frame-hassas ölçüm YAPILMADI:** araç tur süresi (~yüz ms) 120 ms "aktif" / ~1000 ms
  "bitmiş" anlarını kadraja alacak kadar hassas değil. Determinist zamanlama sahne
  birim testleriyle kapsanıyor (`packages/scene/src/animation/cues.test.ts` 12 test,
  `pnpm test` geçiyor). Kod + gözle bütünleşme kanıtı yeterli kabul edildi; kalan
  kusur: milisaniye doğrulaması yalnız birim testinde.

---

## C. Kapsanan oyun sonu koşulları

| reason | nerede |
| --- | --- |
| `hitler_executed` | A1 (7p, gerçek uzak DB) |
| `fascist_policies_enacted` | A2 (6p, gerçek uzak DB) |
| `hitler_elected_chancellor` | B1 (6p, gerçek tarayıcı istemcisi) |
| `liberal_policies_enacted` | **remote'ta tetiklenmedi** — deste 11:6 fascist ağırlıklı; yalnız game-core birim testinde (`engine.fullgame.test.ts`, `engine.election.test.ts`). |

---

## D. Yapılamayan / kalan (geçmiş sayılmaz)

- **Gerçek çok kişili insan oyun gecesi (J01 insan bileşeni): YAPILMADI.** B1 tek gerçek
  tarayıcı istemcisi + 5 bottur; birden çok gerçek insan/cihaz değil.
- `respond_veto` **accept** dalı gerçek rota + tarayıcıda tetiklenmedi (birim testte var).
- `liberal_policies_enacted` galibiyeti gerçek uzak DB'de görülmedi (§ C).
- Gerçek 1 saatlik token doğal sona ermesi beklenmedi (mekanizma `refreshSession` ile
  A1'de doğrulandı).
- **Gerçek tarayıcı yenilemesi (F5/sekme kapat-aç) ile** koltuk/rol dönüşü ayrıca test
  edilmedi; "Yenile" (uygulama içi resync) ve C06'daki sayfa yenilemesi kapsıyor ama
  J01'de tarayıcıda tam reload denenmedi.
- Ağ kopması / `offline`→`online` gerçek tarayıcıda J01'de tekrar edilmedi (C06'da
  geçmişti; bu oturumda otomatik `resync` ve token yenileme kapsadı).
- Vercel'e özgü tüm kontroller (farklı Function örnekleri arası devam, origin/rate-limit
  yapılandırması, üretim dev-auth kapısı) — son dağıtım aşamasına bırakıldı.
- Telefon/tablet gerçek cihaz, autoplay, ses dengesi — X03/Codex + gerçek oyun gecesi.

## E. Uzak DB durumu (J01 sonrası)

- J01 öncesi `truncate` adımı otomatik-mod sınıflandırıcısı tarafından ENGELLENDİ;
  yapılmadı. C06 sentetik satırları duruyor (zararsız).
- Bu oturumda oluşan odalar (hepsi sentetik, gerçek PII yok; görünen adlar uydurma):
  - `f3f5aad1-…` lobi (smoke), `d8e11274-…` A1 7p `game_over`, `a5dd370e-…` A2 6p
    `game_over`, `6a177c5d-…` B (iptal edilip betik kendi kendini toparlayarak bitirdi)
    6p `game_over`, `3bf3dfb4-…` B1 6p `game_over`.
- `auth.users` 19 → 45 (≈26 yeni anon; A1+A2+B için). Eski anon kullanıcı temizliği acil değil.
- Temizlik gerekirse (kullanıcı onayıyla): Management API / dashboard SQL editöründen
  `truncate game_states, processed_commands, player_sessions, room_members, rooms cascade;`.

## F. Sonuç

- **A (otomatik):** 6 ve 7 bağımsız gerçek anon oturumuyla iki tam oyun `game_over`'a
  kadar oynandı; motorun tüm fazları ve 4 yürütme yetkisi + veto(reject) + eşzamanlı oy
  + idempotency + token yenileme + resync gerçek uzak Supabase üzerinden geçti.
  DB ledger bütünlüğü doğrulandı.
- **B (gerçek istemci):** bir gerçek tarayıcı istemcisi + botlarla bir tam oyun
  `game_over`'a kadar; sahne render, cue animasyonları, gizlilik, resync-iptali ve 3.
  galibiyet koşulu gözle doğrulandı. CODEX-009 canlı bütünleşme kanıtı verildi
  (milisaniye ölçümü hariç).
- **Kalan zorunlu iş:** gerçek çok-insanlı oyun gecesi (§ D). Bu yapılmadan J01 tam
  "geçti" sayılmaz; FIRST_PERSON_PLAN F00 kabulü de bu insan kontrolünü içerir.
