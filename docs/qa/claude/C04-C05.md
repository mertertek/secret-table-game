# C04 / C05 QA — giriş/lobi/oyun ekranları + canlı 3D entegrasyonu

Tarih: 2026-09-09. Sahip: Claude. Ortam: yerel (`pnpm dev`, dev kimliği + `InMemoryGateway`).
Uzak Supabase/Vercel yok → gerçek Realtime, token yenileme, çok-tarayıcı testi C06/J01.

## Ne değişti

- **Blocker düzeltmesi:** `apps/web/api/_lib/context.ts` — `dev:` kimliği + bellek
  gateway'i düşüşü artık yalnız yerel geliştirmede (`VERCEL` yok, `NODE_ENV !==
  'production'`) veya açık `SECRET_TABLE_ALLOW_DEV_AUTH=1` ile. Vercel'de Supabase
  eksikse `503 SUPABASE_NOT_CONFIGURED`. Birim testi: `context.test.ts` (4 senaryo).
- **Lobi görünümü:** `LobbySnapshot` tipi (`packages/contracts/src/lobby.ts`, additive),
  `GameService.getLobby()`, `POST /api/game` `lobby_view` eylemi. `start_game` artık
  tüm aktif üyeler hazır değilse `NOT_ALLOWED` (ikinci savunma; UI düğmeyi de kapatır).
- **C04 ekranlar:** `EntryPage` (isim + oda aç / davet kodu), `JoinPage` (`/katil/:code`),
  `RoomPage` (`/oda/:roomId` — duruma göre lobi/oyun), `RootErrorBoundary`. `ui/`:
  `Lobby`, `GameScreen`, `ActionPanel` (AllowedAction → düğme + seç/onayla/gönder),
  `RolePanel` (varsayılan kapalı, odak yönetimi), `PlayersPanel`, `TableStatus`,
  `HistoryLog`, `SceneFrame` (boyutlu kap + Suspense + WebGL hata sınırı), `text.ts`
  (labelKey/hata/aşama TR sözlüğü), `usePrefs` (kalite/hareket/ses, localStorage).
- **C05 canlı döngü:** `multiplayer/useRoomState.ts` — oturum → `lobby_view`/`view` →
  sürüm sinyali (`watchRoomRevision`) → yeniden al; heartbeat 20 sn; `claim_control`
  bir kez; lobi yoklaması 3 sn; `resync` ilk oyun görünümünde. `adapters/sceneView.ts`
  — cue kuyruğu (`cueId` tekrarını engelle, `resync`/oyun değişince sıfırla),
  `reconcileSelection` (phaseId/aksiyon değişince yerel seçimi düşür).
- Üretim rotası artık gerçek oyun ekranını içeriyor (`routes.tsx`; `/dev/scene` hâlâ
  yalnız `import.meta.env.DEV`).

## Geçen kontroller

- `pnpm typecheck` → 6/6 paket temiz.
- `pnpm test` → **142 test** (contracts 6, fixtures 28, game-core 65, scene 11,
  server 28 [+5 lobby], web 4 [YENİ context kapısı]).
- `pnpm --filter @secret-table/web build` → `apps/web/dist`, 698 modül. Tarayıcı
  paketinde `@secret-table/server` / `game-core` iç sembolleri / `SUPABASE_SECRET_KEY`
  YOK (üretilen `index-*.js` üzerinde `grep` ile doğrulandı). Sahne fontları
  (`NotoSans`, `NotoSerif` .ttf) `dist/assets/` içine paketlendi.
- **HTTP API uçtan uca (dev kipi, script):** `create_room` → 4× `join_room` →
  `lobby_view` (`canStart=false`; üye listesinde `userId` sızıntısı yok; host/`isLocal`
  doğru) → hazır olmadan `start_game` = `NOT_ALLOWED` → 5× `set_ready` → `canStart=true`
  → `start_game` → `lobby_view.status='in_game'` → her oyuncu `view` (yalnız kendi
  `privateView.role`; herkese açık `players[]` içinde `role`/`party` yok) → tüm
  aşamalardan geçerek `game_over` (fascist / `hitler_elected_chancellor`, 5 rol açıldı).
  Aynı `commandId` ile tekrar → aynı `revision` (idempotent).
- **Tarayıcı (Claude Browser, 800×450):** `/` giriş → "Yeni oda aç" → `/oda/:id` lobi
  (davet bağlantısı + Kopyala, koltuk/hazır/host rozetleri, "En az 5 oyuncu" ipucu).
  4 bot + hazır + başlat (script) → lobi 3 sn içinde oyun ekranına geçti. 3D masa
  render oldu (masa, koltuklar, avatarlar, isimlikler, iki tahta). Rol onayı:
  "Hazırım" → "Seçilen" → "Gönder" → "yapman gereken bir şey yok". "Yenile" ile
  `voting` aşaması geldi; PlayersPanel'de "Başkan adayı"/"Şansölye adayı"/"oy verdi"
  rozetleri. "Evet" → "Gönder" → oy kaydı. RolePanel "Göster": *Rolün: Hitler*,
  *Bildiklerin: Ceylan — Faşist (Hitler faşistleri bilir)* — 5 kişilik kural doğru.
  Konsol: yalnız bilinen `THREE.Clock` deprecation uyarısı; hata yok.

## Yapılmayan / kalan (C06 · J01 · X03)

- Gerçek Supabase Realtime kanalı, kaçan bildirim toparlama, access token süre dolması
  ve 7 ayrı tarayıcıyla eşzamanlı oturum — dev kipinde Realtime yok, yalnız 12 sn'lik
  yedek HTTP kontrolü + 3 sn lobi yoklaması çalışıyor.
- `connection: 'reconnecting'/'disconnected'` istemci-tarafı tespiti henüz yok
  (`SceneView.connection` sunucudan hep `connected`). Ağ hatası şu an `transient`
  mesaj + sonraki yoklamada toparlama olarak görünüyor. C05/J01'de istemci adaptörü
  bağlantı durumunu düşürecek.
- Lobi üyelik değişikliği (katılım/hazır) için DB tetikleyicisi yok; 3 sn yoklama ile
  yansıyor. Gerçek Supabase'te membership realtime veya kısa yoklama C06'da netleşir.
- X03 cue koreografisi/ses: `cues` HTTP yanıtından `SceneFrame`'e geçiyor; `soundEnabled`
  / `reducedMotion` / `quality` props iletiliyor. Görsel oynatma Codex'te.
- Klavye/ekran okuyucu: tüm eylemler `<button>`; `aria-pressed`/`aria-live`/`role`
  eklendi; `RolePanel` odak yönetimi var. Gerçek ekran okuyucu turu J01'de.
- Küçük ekran: `@media (max-width: 900px)` tek sütun + 45vh sahne; gerçek cihaz X04/J01.
